num_epochs: 10
num_train_sample: 1
validation: true
kl_type: mean
entropy_type: conditional
dataset: cifar10
gamma: 0.0
lambda_info: 0.1
run_id: gamma_${gamma}_lambda_${lambda_info}
base_dir: ./hydra_experiments
